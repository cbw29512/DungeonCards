name: RAW Pregen Monster Certification

on:
  push:
    branches:
      - agent/raw-pregen-monster-certification

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Certify full Fight Cards roster UI
        run: npx vitest run src/components/DndFightCardsArena.test.tsx
      - name: Certify Heroic Crit damage helpers
        run: npx vitest run src/utils/fightRules.test.ts
      - name: Certify battle resolver
        run: npx vitest run src/utils/fightBattle.test.ts
      - name: Run full test suite
        shell: bash
        run: |
          set -o pipefail
          npm test 2>&1 | tee test-output.log
      - name: Preserve test diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: raw-certification-test-output
          path: test-output.log
          if-no-files-found: ignore
          retention-days: 7
      - run: npm run build
