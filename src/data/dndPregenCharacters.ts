import type { DndPregenCharacterRecord } from "../types/dndPregenCharacter";

/**
 * Only complete, rules-reviewed, playtested, and verified characters belong
 * in the public catalog. Draft generation fixtures must stay outside this file.
 */
export const dndPregenCharacters: DndPregenCharacterRecord[] = [];
