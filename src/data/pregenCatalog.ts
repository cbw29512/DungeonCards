import type { PregenCharacter } from "../types/pregens";

/**
 * Public pregens enter this catalog only after rules review, playtesting, and
 * verification. Draft records belong in generation fixtures, not production.
 */
export const pregenCatalog: PregenCharacter[] = [];
